import { Component } from '@angular/core';

@Component({
  selector: 'app-rnr',
  standalone: true, // Optional if using standalone components
  imports: [],
  templateUrl: './rnr.html',
  styleUrls: ['./rnr.css'] // ✅ changed styleUrl → styleUrls
})
export class Rnr {
  // Component logic can go here
}
